##################################################
{ pkgs
}:

##################################################
{ resume
}:

assert (builtins.isString resume);

##################################################
pkgs.writeTextFile 

  { name = "resume.tex";
    text = resume;
  }

##################################################